export type Video = {
    user_id: string;
    description: string;
    video_url: string;
    title: string;
}

export type VideoData = {
    user_id: string;
    description: string;
    video_url: string;
    title: string;
    id: string;
    created_at: string;
    num_comments: number
}